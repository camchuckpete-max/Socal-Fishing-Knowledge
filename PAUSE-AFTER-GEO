Hold the chain when the geographic ladder finishes.

Cameron's checkpoint, 2026-08-26. The geo phase is the layer the whole plan was
re-cut around: 83 rungs, 76 zones, 391 charted spots. When the last rung lands,
review-chunk.yml's preflight sees a non-geo phase, finds this file, and stops
the chain rather than rolling into the ~37-chunk transform tail.

This is NOT gate B. It is a place to look at the finished map and decide whether
the tail is worth running next.

To resume: delete this file, commit, push. The next trampoline firing (2-hourly)
picks the chain back up on its own; nothing else needs changing.

To stop everything instead, at any time: create a file named STOP at the branch
root. That is the kill switch and it outranks this one.
