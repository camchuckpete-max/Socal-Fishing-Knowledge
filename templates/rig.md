# Template: rigging note (`type: rig`, STANDARD tier)

Knots, leaders, terminal rigs: parameters + judgment, with the source video
linked for the visual steps. Never step-by-step transcription — paraphrase
(CLAUDE.md, unchanged).

## Front matter

```yaml
---
type: rig
tags: [dropper-loop, ...]
sources: [<video_id>, ...]
confidence: high
regions: [socal-bight, ...]
waters: [island, bank, ...]
layout: v2
line_class: 40-60lb mono / 65lb braid   # `unknown` where the corpus is silent
hook_sizes: 2/0-4/0
---
```

## Skeleton (canonical order — extras allowed between)

```
# <Rig / knot name>

<Lead: what it is, what it's for.>

## When to use
The situations that call for this rig over its alternatives.

## Parameters
Line/leader classes, hook sizes, weights, lengths — the judgment calls, with
the reasons. Video link for the visual tying steps.

<free sections: variations, failure modes — as needed.>

## Evidence
One line linking evidence/<note>.md (only when observations exist).

## Linked from   (machine-generated)
```
